{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "ELECTRON_EXTRA_LAUNCH_ARGS=--remote-debugging-port=9222 npx cypress run",
    "percy:open": "percy exec -- cypress open",
    "percy:run": "ELECTRON_EXTRA_LAUNCH_ARGS=--remote-debugging-port=9222 percy exec -- cypress run"
  },
  "devDependencies": {
    "@chromatic-com/cypress": "^0.6.6",
    "@percy/cli": "^1.28.3",
    "@percy/cypress": "^3.1.2",
    "chromatic": "^11.3.0",
    "cypress": "^13.8.0"
  }
}
